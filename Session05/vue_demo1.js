var app = new Vue({
    // which element going to manipulate el stands for element then name of element, which this case id=app-1, div
    el: '#app-1',
    data: {
        message: 'Hello Vue!'
    }
});